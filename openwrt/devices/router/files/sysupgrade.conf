## This file contains files and directories that should
## be preserved during an upgrade.

# /srv/tftp images are too big for the backup tmpfs (initrd alone > /tmp);
# rerun this preserved script after every flash instead
/usr/bin/netboot-update
