watch_file shells/flake-module.nix

use flake

# i prefer the openpgp key because pin entry can be cached with it. age slot is PIV slot and cannot cache pin entry
# # unset: decrypt sops via cached gpg path, not uncached PIV age.
# see docs/decisions/yubikey-openpgp-ssh.md
# export SOPS_AGE_KEY_CMD="age-plugin-yubikey --identity"
