name: renovate
on:
  schedule:
    - cron: "0 1 * * *"
  workflow_dispatch:
concurrency:
  group: renovate
  cancel-in-progress: false
jobs:
  renovate:
    runs-on: nixworker
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
      - name: renovate
        env:
          RENOVATE_PLATFORM: forgejo
          RENOVATE_ENDPOINT: ${{ forgejo.server_url }}
          RENOVATE_REPOSITORIES: ${{ forgejo.repository }}
          RENOVATE_TOKEN: ${{ secrets.RENOVATE_TOKEN }}
          RENOVATE_GITHUB_COM_TOKEN: ${{ secrets.RENOVATE_GITHUB_COM_TOKEN }}
          RENOVATE_ALLOWED_COMMANDS: '["^bash packages/live-ocr/update-vendor-hash\\.sh$"]'
          # use tools from PATH (not containerbase); gomodTidy needs go and
          # update-vendor-hash.sh needs nix, both provided by the nix shell.
          RENOVATE_BINARY_SOURCE: global
          LOG_LEVEL: info
        run: nix shell nixpkgs#go --command nix run nixpkgs#renovate
